# Scripts that are executed on the Linux HPC must keep LF line endings.
# (A CRLF .slurm file fails on Artemis with "bad interpreter: No such file".)
*.slurm         text eol=lf
*.sh            text eol=lf
analysis/server/hpc text eol=lf
analysis/server/*.R text eol=lf
